# Admin authentication
ADMIN_USERNAME=kmarafatislam@gmail.com
ADMIN_PASSWORD=1234
ADMIN_SESSION_SECRET=change-this-random-long-secret

# Storage key in KV
CONTENT_KV_KEY=portfolio:content

# Optional: if Vercel KV is configured in project, @vercel/kv reads env automatically.
# For local fallback, data is written to data/content.runtime.json.
